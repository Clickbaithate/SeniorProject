import { useState } from 'react'
import './App.css'

// In terminal, cd to client folder then cine-vault folder and run "npm i" then run "npm run dev" to start frontend
function App() {
  return (
    <h1 className="text-3xl font-bold underline">
      Hello world!
    </h1>
  )
}

export default App;
